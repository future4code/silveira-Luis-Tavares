// A) criamos uma função para inserir os dados de usuário na tabela "User" do banco de dados, com o id, email e senha passado por parâmetro na chamda da função.
// B)

/*

CREATE TABLE User (
    id INT PRIMARY KEY AUTO_INCREMENT,
    email VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);

*/

// C) ./data/UserDatabase.ts