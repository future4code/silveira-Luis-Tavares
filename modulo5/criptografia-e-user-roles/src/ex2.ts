// A) primeiramente o cadastro, pois necessitamos criptografar a senha quando o usuário cadastrá-la

// B) - C) => ./endpoint/UserController.ts

// D) acredito que não, pois na função de getProfile pegamos apenas as infos de id e email através do token do usuário