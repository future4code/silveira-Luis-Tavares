export type User = {
    id: number,
    name: string,
    email: string,
    password: string
};

export type Product = {
    id: number,
    name: string,
    price: number,
    image_url: string
};